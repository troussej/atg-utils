#!/usr/bin/env node

'use strict';
const program = require('commander');


program
    .version('{{version}}')
    {{#commands}}
    .command("{{& command}}","{{description}}")
    {{/commands}}
    .parse(process.argv);

